# fastify-cli bug

```console
npm run dev
# check that the server is working
mkdir -p fooo/bar/baz/bing
# now see that that the server never restarts
# Change back to fastify-cli 4.1.1 and restart the server.
# The above action does not crash that versions server.
```


